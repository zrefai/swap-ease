{
  "name": "@swap-ease/data",
  "version": "1.0.2",
  "description": "A package containing common data patterns and models used across different services within swap-ease",
  "author": "Zaki Refai",
  "license": "ISC",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf ./dist",
    "format": "npx prettier --write src",
    "build": "npm-run-all clean format && tsc && tsup"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.1.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.4"
  },
  "prettier": {
    "singleQuote": true
  }
}
